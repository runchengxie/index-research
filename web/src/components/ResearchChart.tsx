import { useEffect, useRef } from "react";
import * as echarts from "echarts";
import type { Row } from "../api";

export function NavChart({ rows }:{rows:Row[]}) { return <Chart option={{tooltip:{trigger:"axis"},grid:{left:48,right:24,top:24,bottom:44},xAxis:{type:"category",data:rows.map(r=>r.date),axisLabel:{formatter:(v:string)=>v.slice(0,4),hideOverlap:true}},yAxis:{type:"value",scale:true},series:[{name:"累计净值",type:"line",showSymbol:false,smooth:true,data:rows.map(r=>Number(r.nav)),lineStyle:{width:2.5,color:"#1267d6"},areaStyle:{color:"rgba(18,103,214,.08)"}}]}} />; }
export function AnnualChart({ rows }:{rows:Row[]}) { return <Chart option={{tooltip:{trigger:"axis",formatter:(params:any)=>{const item=Array.isArray(params)?params[0]:params;return `${item.axisValue}<br/>年度收益：${(Number(rows[item.dataIndex].return)*100).toFixed(2)}%`}},grid:{left:48,right:24,top:24,bottom:44},xAxis:{type:"category",data:rows.map(r=>r.year),axisLabel:{interval:2}},yAxis:{type:"value",axisLabel:{formatter:"{value}%"}},series:[{type:"bar",data:rows.map(r=>({value:Number(r.return)*100,itemStyle:{color:Number(r.return)>=0?"#1267d6":"#c53a37"}}))}]}} />; }
export function RankChart({ rows }:{rows:Row[]}) { const top=[...rows].sort((a,b)=>Number(b.cagr)-Number(a.cagr)).slice(0,12); return <Chart option={{tooltip:{trigger:"axis",axisPointer:{type:"shadow"},formatter:(params:any)=>{const item=Array.isArray(params)?params[0]:params;return `${item.name}<br/>十年年化价格回报：${Number(item.value).toFixed(1)}%`}},grid:{left:140,right:32,top:12,bottom:24},xAxis:{type:"value",axisLabel:{formatter:"{value}%"}},yAxis:{type:"category",data:top.map(r=>r.indx_name).reverse(),axisLabel:{width:120,overflow:"truncate"}},series:[{type:"bar",data:top.map(r=>Number(r.cagr)*100).reverse(),itemStyle:{color:"#1267d6"}}]}} />; }
function Chart({option}:{option:echarts.EChartsOption}) { const ref=useRef<HTMLDivElement>(null); useEffect(()=>{if(!ref.current)return;const chart=echarts.init(ref.current);chart.setOption(option);const resize=()=>chart.resize();window.addEventListener("resize",resize);return()=>{window.removeEventListener("resize",resize);chart.dispose()};},[option]); return <div ref={ref} className="research-chart" role="img" />; }
